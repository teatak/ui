import { Meta } from '@storybook/addon-docs/blocks'

<Meta title='Config' />

### Theme

```typescript
import type { Preview } from "@storybook/react-webpack5";
import { createTheme } from '@/style'

declare module '@/style' {
  interface ColorNameOverrides {
    blue: true
  }
}

const theme = createTheme({
  color: {
    blue: {
      base: "rgb(10, 132, 255)",
      10: "255, 255, 255"
    }
  },
  colorSchemes: {
    light: {
      blue: {
        main: "rgb(var(--tui-danger-5, 219, 242, 255))"
      }
    }
  }
})
```