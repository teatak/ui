import React, { useRef } from 'react'
import { Button } from '../components'

export default {
    title: 'Components/Button',
    component: Button,
    parameters: {
    },
    tags: ['autodocs'],
    argTypes: {
    },
}

export const Simple = {
    args: {
        children: 'Button'
    }
}
